import React from 'react';
import { Link, Router } from 'react-router-dom';
import TaskHeader from './task_header';
import { connect } from 'react-redux';

export default connect(null, null)(TaskHeader);
